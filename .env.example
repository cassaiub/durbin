# Optional live Google Calendar synchronization for /events.
# The calendar must be public. Restrict the browser API key to the
# durbin.cassa.bd origin and the Google Calendar API.
PUBLIC_GOOGLE_CALENDAR_ID=
PUBLIC_GOOGLE_CALENDAR_API_KEY=
